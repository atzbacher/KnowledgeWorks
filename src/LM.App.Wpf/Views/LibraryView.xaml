<UserControl x:Class="LM.App.Wpf.Views.LibraryView"
             x:Name="LibraryViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:LM.App.Wpf.Views.Behaviors"
             xmlns:converters="clr-namespace:LM.App.Wpf.Views.Converters"
             xmlns:viewModels="clr-namespace:LM.App.Wpf.ViewModels.Library"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:controls="clr-namespace:LM.App.Wpf.Views.Library.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Library/LibraryCommonResources.xaml" />
        <ResourceDictionary Source="Library/LibraryThemeResources.xaml" />
        <ResourceDictionary Source="Library/LibraryEntryDetailTemplate.xaml" />
        <ResourceDictionary Source="Templates/LibraryResultsColumns.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>

  </UserControl.Resources>
  <Grid Background="{StaticResource LibrarySurfaceBackgroundBrush}">
    <Border Margin="12"
            Background="{StaticResource LibraryPanelBackgroundBrush}"
            CornerRadius="{StaticResource LibraryCardCornerRadius}">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="{Binding DataContext.Filters.LeftPanelWidth, ElementName=LibraryViewControl, Mode=TwoWay}" MinWidth="0"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="{Binding DataContext.Filters.RightPanelWidth, ElementName=LibraryViewControl, Mode=TwoWay}" MinWidth="0"/>
        </Grid.ColumnDefinitions>

    <!-- Search header -->
    <Border Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="5"
            Background="{StaticResource LibraryPanelBackgroundBrush}"
            BorderBrush="{StaticResource LibraryPanelBorderBrush}"
            BorderThickness="0,0,0,1"
            CornerRadius="{StaticResource LibraryCardCornerRadius}"
            Padding="16,12">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="0,0,12,0">
          <Button Command="{Binding Filters.ToggleLeftPanelCommand}"
                  AutomationProperties.Name="Toggle saved searches panel"
                  Width="32"
                  Height="32"
                  Padding="0"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="14">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource LibraryHeaderButtonStyle}">
                <Setter Property="Content" Value="&#xE1D1;" />
                <Setter Property="ToolTip" Value="Hide saved searches" />
                <Style.Triggers>
                  <DataTrigger Binding="{Binding Filters.IsLeftPanelCollapsed}" Value="True">
                    <Setter Property="Content" Value="&#xE1D0;" />
                    <Setter Property="ToolTip" Value="Show saved searches" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
          </Button>
        </StackPanel>

        <StackPanel Grid.Column="1">
          <TextBlock Text="Library search"
                     FontSize="16"
                     FontWeight="SemiBold"
                     Foreground="{StaticResource LibraryPrimaryForegroundBrush}" />
          <controls:LibrarySearchQueryBox Margin="0,8,0,0"
                                          MinHeight="44"
                                          Text="{Binding Filters.UnifiedQuery}"
                                          ToolTip="{Binding Filters.KeywordTooltip}"
                                          ToolTipService.ShowDuration="60000"
                                          AutomationProperties.Name="Library search query" />
          <StackPanel Orientation="Horizontal"
                      Margin="0,8,0,0"
                      VerticalAlignment="Center">
            <ToggleButton Style="{StaticResource LibraryPillToggleStyle}"
                          IsChecked="{Binding Filters.UseFullTextSearch}"
                          Width="52"
                          Height="26"
                          Margin="0,0,8,0"
                          AutomationProperties.Name="Full-text ranking toggle"
                          ToolTip="Toggle semantic full-text ranking" />
            <TextBlock Foreground="{StaticResource LibrarySecondaryForegroundBrush}"
                       VerticalAlignment="Center"
                       Text="Full-text ranking" />
          </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center">
          <Button Style="{StaticResource LibraryHeaderButtonStyle}"
                  Width="32"
                  Height="32"
                  Padding="0"
                  Margin="0,0,8,0"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="14"
                  Content="&#xE785;"
                  Command="{Binding Filters.ClearCommand}"
                  ToolTip="Clear query"
                  AutomationProperties.Name="Clear search" />
          <Button Style="{StaticResource LibraryHeaderButtonStyle}"
                  Width="32"
                  Height="32"
                  Padding="0"
                  Margin="0,0,8,0"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="14"
                  Content="&#xE74E;"
                  Command="{Binding Filters.SavePresetCommand}"
                  ToolTip="Save search"
                  AutomationProperties.Name="Save search" />
          <Button Style="{StaticResource LibraryHeaderButtonStyle}"
                  Width="32"
                  Height="32"
                  Padding="0"
                  Margin="0,0,8,0"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="14"
                  Content="&#xE8B7;"
                  Command="{Binding Filters.LoadPresetCommand}"
                  ToolTip="Load saved search"
                  AutomationProperties.Name="Load saved search" />
          <Button Style="{StaticResource LibraryHeaderButtonStyle}"
                  Width="32"
                  Height="32"
                  Padding="0"
                  Margin="0,0,8,0"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="14"
                  Content="&#xE71D;"
                  Command="{Binding Filters.ManagePresetsCommand}"
                  ToolTip="Manage presets"
                  AutomationProperties.Name="Manage saved searches" />
          <Button Style="{StaticResource LibraryPrimaryButtonStyle}"
                  Width="36"
                  Height="36"
                  Padding="0"
                  Margin="0,0,8,0"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="16"
                  Content="&#xE768;"
                  Command="{Binding SearchCommand}"
                  ToolTip="Run search"
                  AutomationProperties.Name="Run search" />
          <Button Command="{Binding Filters.ToggleRightPanelCommand}"
                  AutomationProperties.Name="Toggle entry detail panel"
                  Width="32"
                  Height="32"
                  Padding="0"
                  FontFamily="Segoe MDL2 Assets"
                  FontSize="14">
            <Button.Style>
              <Style TargetType="Button" BasedOn="{StaticResource LibraryHeaderButtonStyle}">
                <Setter Property="Content" Value="&#xE90F;" />
                <Setter Property="ToolTip" Value="Hide entry details" />
                <Style.Triggers>
                  <DataTrigger Binding="{Binding Filters.IsRightPanelCollapsed}" Value="True">
                    <Setter Property="Content" Value="&#xE1CE;" />
                    <Setter Property="ToolTip" Value="Show entry details" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Button.Style>
          </Button>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Navigation tree -->
    <Border Grid.Row="1"
            Grid.Column="0"
            Background="{StaticResource LibraryPanelSubtleBrush}"
            BorderBrush="{StaticResource LibraryPanelBorderBrush}"
            BorderThickness="0,1,1,0">
      <ScrollViewer VerticalScrollBarVisibility="Auto" DataContext="{Binding Filters}">
        <StackPanel Margin="12">
          <Border Style="{StaticResource LibrarySectionCardStyle}" Background="{StaticResource LibraryPanelBackgroundBrush}">
            <StackPanel>
              <TextBlock Text="Saved searches"
                         FontWeight="SemiBold"
                         FontSize="14"
                         Foreground="{StaticResource LibraryPrimaryForegroundBrush}"
                         Margin="0,0,0,8"/>
              <TreeView ItemsSource="{Binding NavigationRoots}" SelectedItemChanged="OnNavigationSelected">
            <TreeView.ItemTemplate>
              <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                <StackPanel>
                  <TextBlock Text="{Binding Name}">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock">
                        <Setter Property="FontWeight" Value="Normal"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding Kind}" Value="{x:Static viewModels:LibraryNavigationNodeKind.Category}">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>
                  <TextBlock Text="{Binding Subtitle}" FontSize="11" Foreground="{StaticResource LibraryMutedForegroundBrush}">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding Subtitle}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                          </DataTrigger>
                          <DataTrigger Binding="{Binding Subtitle}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>
                </StackPanel>
              </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
              </TreeView>
            </StackPanel>
          </Border>
          <Border Style="{StaticResource LibrarySectionCardStyle}"
                  Background="{StaticResource LibraryPanelBackgroundBrush}"
                  Margin="0,12,0,0"
                  Visibility="{Binding UseFullTextSearch, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
              <TextBlock Text="Semantic keywords"
                         FontWeight="SemiBold"
                         Foreground="{StaticResource LibraryPrimaryForegroundBrush}" />
              <TextBlock Text="Add terms to refine ranking"
                         Margin="0,4,0,8"
                         FontSize="11"
                         Foreground="{StaticResource LibraryMutedForegroundBrush}" />
              <TextBox Text="{Binding FullTextQuery, UpdateSourceTrigger=PropertyChanged}"
                       Padding="10"
                       Background="{StaticResource LibraryPanelSubtleBrush}"
                       BorderBrush="{StaticResource LibraryPanelBorderBrush}"
                       BorderThickness="1"
                       AutomationProperties.Name="Full-text ranking keywords" />
              <WrapPanel Margin="0,8,0,0">
                <CheckBox Content="Title"
                          Margin="0,0,12,8"
                          Foreground="{StaticResource LibraryPrimaryForegroundBrush}"
                          IsChecked="{Binding FullTextInTitle}" />
                <CheckBox Content="Abstract"
                          Margin="0,0,12,8"
                          Foreground="{StaticResource LibraryPrimaryForegroundBrush}"
                          IsChecked="{Binding FullTextInAbstract}" />
                <CheckBox Content="Content"
                          Margin="0,0,12,8"
                          Foreground="{StaticResource LibraryPrimaryForegroundBrush}"
                          IsChecked="{Binding FullTextInContent}" />
              </WrapPanel>
            </StackPanel>
          </Border>
        </StackPanel>
      </ScrollViewer>
    </Border>


    <Button Grid.Row="1"
            Grid.Column="1"
            Command="{Binding DataContext.Filters.ToggleLeftPanelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Width="32"
            Height="48"
            Padding="0"
            Margin="0,0,6,0"
            FontFamily="Segoe MDL2 Assets"
            FontSize="14">
      <Button.Style>
        <Style TargetType="Button" BasedOn="{StaticResource LibraryHeaderButtonStyle}">
          <Setter Property="Content" Value="&#xE0E2;" />
          <Setter Property="ToolTip" Value="Hide saved searches panel" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding DataContext.Filters.IsLeftPanelCollapsed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
              <Setter Property="Content" Value="&#xE0E3;" />
              <Setter Property="ToolTip" Value="Show saved searches panel" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </Button.Style>
    </Button>
    <GridSplitter Grid.Row="1"
                  Grid.Column="1"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Stretch"
                  Background="{StaticResource LibraryPanelBorderBrush}"
                  ShowsPreview="True"
                  Width="4">
      <GridSplitter.Style>
        <Style TargetType="GridSplitter">
          <Setter Property="Visibility" Value="Visible" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding DataContext.Filters.IsLeftPanelCollapsed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
              <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </GridSplitter.Style>
    </GridSplitter>


    <!-- Results -->
    <Grid Grid.Row="1" Grid.Column="2" DataContext="{Binding Results}" Margin="16">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
        <TextBlock Text="Results"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource LibraryPrimaryForegroundBrush}"/>
        <TextBlock Text="(Full-text ranking)"
                   Margin="8,4,0,0"
                   Foreground="{StaticResource LibraryMutedForegroundBrush}"
                   Visibility="{Binding ResultsAreFullText, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <Menu Margin="12,0,0,0"
              VerticalAlignment="Center"
              Foreground="{StaticResource LibrarySecondaryForegroundBrush}"
              DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
          <MenuItem Header="Columns"
                    ItemsSource="{Binding ColumnOptions}">
            <MenuItem.ItemContainerStyle>
              <Style TargetType="MenuItem">
                <Setter Property="Header" Value="{Binding DisplayName}" />
                <Setter Property="IsCheckable" Value="True" />
                <Setter Property="IsChecked" Value="{Binding IsVisible, Mode=TwoWay}" />
              </Style>
            </MenuItem.ItemContainerStyle>
          </MenuItem>
        </Menu>
      </StackPanel>

      <Border Grid.Row="1" Style="{StaticResource LibrarySectionCardStyle}">
        <DataGrid ItemsSource="{Binding Items}"
                  SelectedItem="{Binding Selected}"
                  IsReadOnly="True"
                  AllowDrop="True"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  Background="Transparent"
                  BorderThickness="0"
                  Foreground="{StaticResource LibraryPrimaryForegroundBrush}"
                  RowBackground="{StaticResource LibraryPanelSubtleBrush}"
                  AlternatingRowBackground="{StaticResource LibraryPanelBackgroundBrush}"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  Margin="0">
        <DataGrid.ColumnHeaderStyle>
          <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource LibraryPanelSubtleBrush}" />
            <Setter Property="Foreground" Value="{StaticResource LibrarySecondaryForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource LibraryPanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="6,4" />
          </Style>
        </DataGrid.ColumnHeaderStyle>
        <DataGrid.Resources>
          <Style TargetType="DataGridRow">
            <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <Setter Property="ContextMenu">
              <Setter.Value>
                <ContextMenu>
                  <MenuItem Header="Open"
                            Command="{Binding PlacementTarget.Tag.OpenEntryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                            CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                  <MenuItem Header="Open in PDF viewer"
                            Command="{Binding PlacementTarget.Tag.OpenPdfAnnotationsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                            CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                  <MenuItem Header="Open containing folder"
                            Command="{Binding PlacementTarget.Tag.OpenContainingFolderCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                            CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                  <MenuItem Header="Copy citation / DOI"
                            Command="{Binding PlacementTarget.Tag.CopyMetadataCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                            CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                  <MenuItem Header="Copy workspace path"
                            Command="{Binding PlacementTarget.Tag.CopyWorkspacePathCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                            CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                  <Separator />
                  <MenuItem Header="Edit entry"
                            Command="{Binding PlacementTarget.Tag.EditEntryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                            CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                </ContextMenu>
              </Setter.Value>
            </Setter>
          </Style>
        </DataGrid.Resources>
        <i:Interaction.Behaviors>
          <behaviors:FileDropBehavior UseDataGridRowContext="True"
                                      PreviewDragOverCommand="{Binding PreviewDropCommand}"
                                      DropCommand="{Binding DropCommand}" />
          <behaviors:DataGridColumnVisibilityBehavior VisibilityMap="{Binding DataContext.ColumnVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </i:Interaction.Behaviors>
        <i:Interaction.Triggers>
          <i:EventTrigger EventName="SelectionChanged">
            <i:InvokeCommandAction Command="{Binding HandleSelectionChangedCommand}"
                                   CommandParameter="{Binding SelectedItems, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
          </i:EventTrigger>
        </i:Interaction.Triggers>
        <DataGrid.Columns>
          <StaticResource ResourceKey="LibraryResultsColumn.AttachmentIndicator" />
          <StaticResource ResourceKey="LibraryResultsColumn.Title" />
          <StaticResource ResourceKey="LibraryResultsColumn.Score" />
          <StaticResource ResourceKey="LibraryResultsColumn.Source" />
          <StaticResource ResourceKey="LibraryResultsColumn.Type" />
          <StaticResource ResourceKey="LibraryResultsColumn.Year" />
          <StaticResource ResourceKey="LibraryResultsColumn.AddedOn" />
          <StaticResource ResourceKey="LibraryResultsColumn.AddedBy" />
          <StaticResource ResourceKey="LibraryResultsColumn.InternalId" />
          <StaticResource ResourceKey="LibraryResultsColumn.Doi" />
          <StaticResource ResourceKey="LibraryResultsColumn.Pmid" />
          <StaticResource ResourceKey="LibraryResultsColumn.Nct" />
          <StaticResource ResourceKey="LibraryResultsColumn.Authors" />
          <StaticResource ResourceKey="LibraryResultsColumn.Tags" />
          <StaticResource ResourceKey="LibraryResultsColumn.IsInternal" />
          <StaticResource ResourceKey="LibraryResultsColumn.Snippet" />
        </DataGrid.Columns>
        </DataGrid>
      </Border>

      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
        <Button Style="{StaticResource LibraryPrimaryButtonStyle}"
                Width="36"
                Height="36"
                Padding="0"
                FontFamily="Segoe MDL2 Assets"
                FontSize="16"
                Content="&#xE8A7;"
                ToolTip="Open selected entry"
                AutomationProperties.Name="Open selected entry"
                Command="{Binding DataContext.OpenEntryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        <Button Style="{StaticResource LibraryPrimaryButtonStyle}"
                Width="36"
                Height="36"
                Margin="8,0,0,0"
                Padding="0"
                FontFamily="Segoe MDL2 Assets"
                FontSize="16"
                Content="&#xE8A5;"
                ToolTip="Open selected PDF in viewer"
                AutomationProperties.Name="Open selected PDF in viewer"
                Command="{Binding DataContext.OpenPdfAnnotationsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
      </StackPanel>
    </Grid>


    <GridSplitter Grid.Row="1"
                  Grid.Column="3"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Stretch"
                  Background="{StaticResource LibraryPanelBorderBrush}"
                  ShowsPreview="True"
                  Width="4">
      <GridSplitter.Style>
        <Style TargetType="GridSplitter">
          <Setter Property="Visibility" Value="Visible" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding DataContext.Filters.IsRightPanelCollapsed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
              <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </GridSplitter.Style>
    </GridSplitter>
    <Button Grid.Row="1"
            Grid.Column="3"
            Command="{Binding DataContext.Filters.ToggleRightPanelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Width="32"
            Height="48"
            Padding="0"
            Margin="6,0,0,0"
            FontFamily="Segoe MDL2 Assets"
            FontSize="14">
      <Button.Style>
        <Style TargetType="Button" BasedOn="{StaticResource LibraryHeaderButtonStyle}">
          <Setter Property="Content" Value="&#xE0E3;" />
          <Setter Property="ToolTip" Value="Hide entry details panel" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding DataContext.Filters.IsRightPanelCollapsed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
              <Setter Property="Content" Value="&#xE0E2;" />
              <Setter Property="ToolTip" Value="Show entry details panel" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </Button.Style>
    </Button>


    <Border Grid.Row="1"
            Grid.Column="4"
            BorderBrush="{StaticResource LibraryPanelBorderBrush}"
            BorderThickness="1,0,0,0"
            Background="{StaticResource LibraryPanelSubtleBrush}"
            Padding="12"
            CornerRadius="{StaticResource LibraryCardCornerRadius}">
      <ScrollViewer VerticalScrollBarVisibility="Auto"
                    AllowDrop="True"
                    DataContext="{Binding Results}">
        <i:Interaction.Behaviors>
          <behaviors:FileDropBehavior PreviewDragOverCommand="{Binding PreviewDropCommand}"
                                      DropCommand="{Binding DropCommand}" />
        </i:Interaction.Behaviors>
        <Grid>

          <Border Style="{StaticResource LibrarySectionCardStyle}" Padding="0" Background="{StaticResource LibraryPanelBackgroundBrush}">
            <ContentPresenter x:Name="EntryDetailPresenter"
                              Content="{Binding Selected}"
                              ContentTemplate="{StaticResource LibraryEntryDetailTemplate}"
                              Margin="0">

              <ContentPresenter.Style>
                <Style TargetType="ContentPresenter">
                  <Setter Property="Visibility" Value="Visible"/>
                  <Style.Triggers>

                    <Trigger Property="Content" Value="{x:Null}">
                      <Setter Property="Visibility" Value="Collapsed"/>
                    </Trigger>

                  </Style.Triggers>
                </Style>
              </ContentPresenter.Style>
            </ContentPresenter>
          </Border>

          <TextBlock Text="Select an entry to view details."
                     Margin="12"
                     FontStyle="Italic"
                     Foreground="{StaticResource LibraryMutedForegroundBrush}"
                     TextWrapping="Wrap"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Center">
            <TextBlock.Style>
              <Style TargetType="TextBlock">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>

                  <DataTrigger Binding="{Binding Content, ElementName=EntryDetailPresenter}" Value="{x:Null}">

                    <Setter Property="Visibility" Value="Visible"/>
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </TextBlock.Style>
          </TextBlock>
        </Grid>
      </ScrollViewer>
    </Border>
      </Grid>
    </Border>
  </Grid>
</UserControl>
