<UserControl x:Class="LM.App.Wpf.Views.Pdf.PdfAnnotationList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LM.App.Wpf.ViewModels.Pdf"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="280">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="PdfAnnotationTemplates.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <CollectionViewSource x:Key="AnnotationsView"
                            Source="{Binding Annotations}">
        <CollectionViewSource.GroupDescriptions>
          <PropertyGroupDescription PropertyName="PageNumber" />
        </CollectionViewSource.GroupDescriptions>
        <CollectionViewSource.SortDescriptions>
          <scm:SortDescription PropertyName="PageNumber" />
          <scm:SortDescription PropertyName="Title" />
        </CollectionViewSource.SortDescriptions>
      </CollectionViewSource>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid>
    <ListView ItemsSource="{Binding Source={StaticResource AnnotationsView}}"
              SelectedItem="{Binding SelectedAnnotation, Mode=TwoWay}"
              ItemTemplate="{StaticResource PdfAnnotationItemTemplate}"
              SelectionChanged="OnSelectionChanged"
              ScrollViewer.VerticalScrollBarVisibility="Auto">
      <ListView.GroupStyle>
        <StaticResource ResourceKey="PdfAnnotationGroupStyle" />
      </ListView.GroupStyle>
    </ListView>
  </Grid>
</UserControl>
