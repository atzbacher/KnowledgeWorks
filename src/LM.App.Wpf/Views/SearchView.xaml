<UserControl x:Class="LM.App.Wpf.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:common="clr-namespace:LM.App.Wpf.Common"
             mc:Ignorable="d">
  <UserControl.Resources>
    <common:BooleanToOpacityConverter x:Key="AlreadyInDbOpacityConverter"
                                       TrueOpacity="1"
                                       FalseOpacity="0" />
  </UserControl.Resources>

  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="8" />
      <RowDefinition Height="*" />
      <RowDefinition Height="8" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Controls -->
    <StackPanel Grid.Row="0" Orientation="Horizontal">
      <TextBox Width="420"
               Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" />
      <ComboBox Width="200"
                ItemsSource="{Binding Databases}"
                SelectedValuePath="Value"
                DisplayMemberPath="DisplayName"
                SelectedValue="{Binding SelectedDatabase, Mode=TwoWay}" />
      <DatePicker Width="140" SelectedDate="{Binding From}" />
      <DatePicker Width="140" SelectedDate="{Binding To}" />
      <Button Content="Run search" Command="{Binding RunSearchCommand}" />
      <Button Content="Save Search" Command="{Binding SaveSearchCommand}" />
      <Button Content="Load Search" Command="{Binding LoadSearchCommand}" />
      <Button Content="Export Search" Command="{Binding ExportSearchCommand}" />
    </StackPanel>

    <!-- Results -->
    <DataGrid Grid.Row="2"
              ItemsSource="{Binding Results}"
              AutoGenerateColumns="False"
              IsReadOnly="False"
              CanUserAddRows="False">
      <DataGrid.Columns>
        <DataGridCheckBoxColumn Header=""
                                 Binding="{Binding Selected, Mode=TwoWay}"
                                 Width="40" />
        <DataGridTemplateColumn Header="DB" Width="40">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <TextBlock Text=""
                         FontFamily="Segoe MDL2 Assets"
                         HorizontalAlignment="Center"
                         Opacity="{Binding AlreadyInDb, Converter={StaticResource AlreadyInDbOpacityConverter}}"
                         ToolTip="Already in DB" />
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="60" />
        <DataGridTextColumn Header="Authors" Binding="{Binding Authors}" Width="260" />
        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
        <DataGridTextColumn Header="Source" Binding="{Binding JournalOrSource}" Width="200" />
        <DataGridTextColumn Header="DOI/ID" Binding="{Binding Doi}" Width="140" />
        <DataGridHyperlinkColumn Header="Link" Binding="{Binding Url}" Width="140" />
        <DataGridTextColumn Header="DB"
                             Binding="{Binding AlreadyInDb}"
                             Visibility="Collapsed" />
      </DataGrid.Columns>
    </DataGrid>

    <!-- Previous runs (simple) -->
    <GroupBox Grid.Row="4" Header="Previous runs">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="8" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding PreviousRuns}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectedItem="{Binding SelectedPreviousRun, Mode=TwoWay}">
          <DataGrid.Columns>
            <DataGridTemplateColumn Header="★" Width="40">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="★" HorizontalAlignment="Center" Foreground="Goldenrod">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock">
                        <Setter Property="Opacity" Value="0.2" />
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                            <Setter Property="Opacity" Value="1" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="When"
                                 Binding="{Binding RunUtc, StringFormat=\{0:yyyy-MM-dd HH:mm\}}"
                                 Width="160" />
            <DataGridTextColumn Header="Provider"
                                 Binding="{Binding Provider}"
                                 Width="100" />
            <DataGridTextColumn Header="Name"
                                 Binding="{Binding DisplayName}"
                                 Width="200" />
            <DataGridTextColumn Header="Query"
                                 Binding="{Binding Query}"
                                 Width="*" />
            <DataGridTextColumn Header="From"
                                 Binding="{Binding From, StringFormat=\{0:yyyy-MM-dd\}}"
                                 Width="120" />
            <DataGridTextColumn Header="To"
                                 Binding="{Binding To, StringFormat=\{0:yyyy-MM-dd\}}"
                                 Width="120" />
            <DataGridTextColumn Header="Total"
                                 Binding="{Binding TotalHits}"
                                 Width="80" />
          </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
          <TextBlock Text="Custom name:" VerticalAlignment="Center" />
          <TextBox Width="240"
                   Margin="8,0,0,0"
                   IsEnabled="{Binding HasSelectedRun}"
                   Text="{Binding SelectedRunDisplayName, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
          <CheckBox Content="Favorite"
                    Margin="12,0,0,0"
                    IsEnabled="{Binding HasSelectedRun}"
                    IsChecked="{Binding SelectedRunIsFavorite}" />
        </StackPanel>
      </Grid>
    </GroupBox>
  </Grid>
</UserControl>
